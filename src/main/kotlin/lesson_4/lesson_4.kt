/*
fun main() {

    // логические операторы
    // операторы сравнения

//    val a = 1 + 1
//    val b = (1 == 1)
    // <, >, >=, <=, ==, !=
    // true (истина)
    // false (ложь)

    // ===, !== (операторы ссылочного сравнения)

    // &&, ||, ! (логические операторы)

    val userAge = 18
//    val comparisonResult: Boolean = (userAge >= AGE_OF_MAJORITY) && (userAge <=RETIREMENT_AGE) // данные сначало сравниваются, а потом присваиваются
//    val comparisonResult: Boolean = (userAge >= AGE_OF_MAJORITY) && !(userAge >=RETIREMENT_AGE) // данные сначало сравниваются, а потом присваиваются
    val comparisonResult: Boolean = userAge in AGE_OF_MAJORITY .. RETIREMENT_AGE
    println("Результат проверки пользователя: $comparisonResult")

//    println(0 < -1) // false
//    println(1 == 1) // true
//    println(2 == 3) // false
//    println(7 != 8) // true

//    val a = false
//    val b = !a
//    println(b)

}

const val RETIREMENT_AGE = 65
const val AGE_OF_MAJORITY = 18

*/
